services:
  web:
    build: ./app
    container_name: flask_app2
    ports:
      - "5001:5001"
    depends_on:
      - redis
    environment:
      - REDIS_HOST=redis

  redis:
    image: redis:7
    container_name: redis_db
    ports:
      - "6379:6379"